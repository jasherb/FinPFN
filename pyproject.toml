[tool.ruff]
target-version = "py310"
line-length = 88
extend-exclude = [
  ".venv",
  "reproduction/runs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["reproduction", "scripts"]
